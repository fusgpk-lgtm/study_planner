
  <!-- メインコンテンツ（中央カード） -->
  <div class="new-student-container">
    <div class="new-student-card">
      <div class="new-student-header">
        <button onclick="alert('生徒一覧画面に戻ります')" class="back-button">
          <i data-lucide="arrow-left" class="icon"></i>
        </button>
        <h1 class="new-student-title">新規生徒の登録</h1>
      </div>

      <p class="new-student-description">
        生徒の基本情報と目標を設定し、カリキュラム作成の準備をします。
      </p>

      <%= form_with(model: @student, url: students_path, class: "signup-form") do |form| %>
        <div>
          <%= form.label :name, "生徒名", class: "form-label" %>
          <%= form.text_field :name, class: "form-input", required: true %>
        </div>

        <div>
          <%= form.label :goal_school, "志望校 / 志望学部", class: "form-label" %>
          <%= form.text_field :goal_school, class: "form-input", required: true %>
        </div>

        <div class="form-grid">
          <div>
            <%= form.label :start_date, "入塾日", class: "form-label" %>
            <%= form.date_field :start_date, class: "form-input", required: true %>
          </div>

          <div>
            <%= form.label :goal_date, "目標受験日", class: "form-label" %>
            <%= form.date_field :goal_date, class: "form-input", required: true %>
          </div>
        </div>

        <%= form.button type: 'submit', class: 'btn-primary submit-button' do %>
          <i data-lucide="save" class="icon"></i>
          生徒を登録し、カリキュラム作成に進む
        <% end %>
      <% end %>
    </div>
  </div>

    <!-- JavaScriptにFirebaseの環境変数を渡すためのスクリプト -->
    <script>
        // window.__app_id = "<%= ENV['FIREBASE_APP_ID'] %>";
        // window.__firebase_config = '<%= ENV['FIREBASE_CONFIG_JSON'] %>';
        // window.__initial_auth_token = "<%= @firebase_auth_token %>";
    </script>
