<div class="teacher-student-detail">
  <h2><%= @student.name %> さんのカリキュラム</h2>
  <p>志望校：<%= @student.goal_school %></p>

  <% if @student.curriculum_template.present? %>
    <div class="template-info">
      <h3>📘 カリキュラムテンプレート：<%= @student.curriculum_template.name %></h3>
      <p class="template-description"><%= @student.curriculum_template.description %></p>
    </div>

    <div class="template-table-wrapper">
      <table class="template-table">
        <thead>
          <tr>
            <th>教材名</th>
            <th>使用開始</th>
            <th>使用終了</th>
          </tr>
        </thead>
        <tbody>
          <% @student.curriculum_template.template_items.includes(:textbook).each do |item| %>
            <tr>
              <td><%= item.textbook.title %></td>
              <td><%= item.start_date.strftime('%Y/%m/%d') %></td>
              <td><%= item.end_date.strftime('%Y/%m/%d') %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p>この生徒にはカリキュラムテンプレートが設定されていません。</p>
  <% end %>
</div>