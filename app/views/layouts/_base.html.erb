<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>StudyPlanner</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- ここを追加：各レイアウト固有のCSSを挿入する口 -->
    <%= yield :extra_css %>
  </head>

  <body>
    <header class="app-header">
      <h1 class="header-title">個別指導カリキュラム管理</h1>

      <div class="header-actions">
        <% if current_user&.teacher? %>
          <%= link_to students_path, class: "home-button" do %>
            <i data-lucide="home" class="icon"></i>
            <span class="text">ホームに戻る</span>
          <% end %>
        <% end %>

        <%= button_to destroy_user_session_path,
                      method: :delete,
                      class: "logout-button",
                      data: { turbo: false } do %>
          <i data-lucide="log-out" class="icon"></i>
          ログアウト
        <% end %>
      </div>
    </header>

    <main class="main-container">
      <%= yield %>
    </main>

    <script>
      document.addEventListener("turbo:load", () => lucide.createIcons());
    </script>
  </body>
</html>